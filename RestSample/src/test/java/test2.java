import org.testng.annotations.Test;

public class test2 {
  @Test
  public void f() {
	 // List<String> allBooks = jsonPathEvaluator.getList("");
//	  for(String book : allBooks)
//	  {
//	  System.out.println("Book: " + book);
//	  }
	//  ArrayList<Map<String,?>> jsonAsArrayList = from(jsonAsString).get("");
	  //JSONObject JSONResponseBody = new JSONObject(response.body().asString());
	    //Get the desired value of a parameter
	  //  String result = JSONResponseBody.getString({ key });
	    // now we count the number of entries in the JSON file, each entry is 1 ride
	    //assertThat(jsonAsArrayList.size(), equalTo(3));
		//  System.out.println("rff-"+jp.get("$"));
	  
	  
//	  JSONArray jar = jsonAsString.getJSONArray("");
//	  System.out.println(jar.length());
	  
	  
//		Object obj = new JSONParser().parse(jsonAsString);
//		JSONObject obj1 = new JSONObject((Map) obj);
	  
	  
	  
//    while (itr2.hasNext()) 
//    {
	  
//    while (itr1.hasNext()) {
//    Map.Entry pair = itr1.next();
//    System.out.println(pair.getKey() + " : " + pair.getValue());
//}
//}
  }
}
